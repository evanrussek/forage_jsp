data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
data %>% filter(grepl("evan-more-like", trial_type))
data <- read.csv('meg_online1.csv')
data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
data %>% filter(grepl("evan-more-like", trial_type)) %>% select(correct)
data <- read.csv('meg_online1.csv')
data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt)
data <- read.csv('meg_online1.csv')
data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt)
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
model_quiz_data$block_number
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
model_quiz_data$block_number %>% unique()
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
model_quiz_data %>% filter(s_num == 1)
data %>% filter(grepl("evan-display-text", trial_type))
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
model_quiz_data %>% filter(s_num == 1)
# don't have the data...
# if you put save block data too early, you'll miss data!
# change this...
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
# don't have the data...
# if you put save block data too early, you'll miss data!
# change this...
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(trial_num > 0)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6)
#
View(task_data)
View(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt)
#
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt)
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, grepl("TEST", phase))  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt)
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, grepl("TEST", phase))  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase)
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, strcmp("TEST", phase))  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase))  %>% # because you labelled block 7 wrong
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase)
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase) %>% arrange(block_number) # no trial number?
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
#
head(task_data)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
#
task_data
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
s1_data <- task_data %>% filter(s_num == 1)
#
task_data
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% group_by(s_num, trigger_val_)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% group_by(s_num, trigger_val_actual, safe_val_actual)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% group_by(s_num, trigger_val_actual, safe_val_actual, other_noise)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% group_by(s_num, trigger_val_actual, safe_val_actual, other_noise)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actuak,
safe_val = safe_val_actual,
other_val = other_val_actual) %>%
group_by(s_num, trigger_val_actual, safe_val_actual, other_noise)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_val_actual) %>%
group_by(s_num, trigger_val_actual, safe_val_actual, other_noise)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val) %>%
summarise(n_trials = n())
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val) %>%
summarise(n_trials = n(), n_accept = sum(accept))
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept))
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n())
#s1_data <- task_data %>% filter(s_num == 1)
?in
5 %in% c(0,1,2,3,4,5)
5 %in% c(0,1,2,3,4)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(s_num)
task_prop_accept$s_num
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(s_num)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(. ~ s_num)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(s_num ~ .)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_wrap(s_num)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_wrap(~s_num)
#s1_data <- task_data %>% filter(s_num == 1)
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger, safe_val_base, trigger_val_base) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# ... get a proportion of this...
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
# really need to look at the subject you scanned...
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_wrap(~s_num)
#s1_data <- task_data %>% filter(s_num == 1)
task_data$safe_val_base
task_data$safe_val_base %>% unique()
task_data$trigger_val_base %>% unique()
task_data$trigger_val_base %>% unique()
tasks_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
task_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
task_data %>% select(s_num == 6) %>% group_by(safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
task_data %>% filter(s_num == 6) %>% group_by(safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
4*4*3
4*4*3*2
task_data$trigger_val_base %>% unique()
task_data %>% filter(s_num == 6) %>% group_by(safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
task_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
task_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
task_data$trigger_val_base %>% unique()
nt_data <- task_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
ggplot(nt_data %>% filter(trigger_val_base > 0, s_num == 6), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
ggplot(nt_data %>% filter(trigger_val_base < 0, s_num == 6), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
nt_data %>% filter(trigger_val_base > 0, s_num == 6)
data %>% filter(s_num == 6, trigger_val_base == 60, p_trigger = .2, safe_val_base = 20)
data %>% filter(s_num == 6, trigger_val_base == 60, p_trigger == .2, safe_val_base = 20)
data %>% filter(s_num == 6, trigger_val_base == 60, p_trigger == .2, safe_val_base == 20)
ggplot(nt_data %>% filter(trigger_val_base < 0, s_num == 7), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
# could the problem be practice trials?
ggplot(nt_data %>% filter(trigger_val_base > 0, s_num == 7), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
# could the problem be practice trials?
ggplot(nt_data %>% filter(trigger_val_base > 0, s_num == 8), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
ggplot(nt_data %>% filter(trigger_val_base < 0, s_num == 8), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
nt_data %>% filter(trigger_val_base > 0, s_num == 8)
nt_data %>% filter(trigger_val_base < 0, s_num == 8)
data %>% filter(s_num == 6, trigger_val_base == -75, p_trigger == .2, safe_val_base == 20)
data %>% filter(s_num == 8, trigger_val_base == -75, p_trigger == .2, safe_val_base == 20)
data %>% filter(s_num == 8, trigger_val_base == -75, p_trigger == .2, safe_val_base == -20)
data %>% filter(s_num == 8, trigger_val_base == -75, p_trigger == .8, safe_val_base == -56)
data %>% filter(s_num == 1, block_number == 9)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger_safe_val_base)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger ,safe_val_base)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger ,safe_val_base) %>% filter(trigger_val_Base == 47.5, p_trigger == .6)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger ,safe_val_base) %>% filter(trigger_val_base == 47.5, p_trigger == .6)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger ,safe_val_base, rt) %>% filter(trigger_val_base == 47.5, p_trigger == .6)
data %>% filter(s_num == 1, block_number == 9) %>% select(trigger_val_base,p_trigger ,safe_val_base, rt, subjectID) %>% filter(trigger_val_base == 47.5, p_trigger == .6)
###### this got coded 3 times... could it just be in the python???????????????????
data <- read.csv('meg_online1b.csv')
data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
data <- read.csv('meg_online1b.csv')
data <- data %>% ungroup() %>% bind_cols(s_num = group_indices(., subjectID))
model_quiz_data <- data %>% filter(grepl("evan-more-like", trial_type)) %>% select(s_num, correct, c1_number, c2_number, outcome_number, choice_number, rt, block_number)
# don't have the data...
# if you put save block data too early, you'll miss data!
# change this...
# just look at the choice data... for each trial, look at the safe , trigger, prob, # accept
task_data <- data %>%
filter(block_number > 6, "TEST" == phase)  %>% # because you labelled block 7 wrong
select(s_num, block_number, trigger_val_actual, safe_val_actual,other_noise, o1_trigger, gl_type, accept, rt, phase, p_trigger, safe_val_base, trigger_val_base) %>% arrange(s_num,block_number) # no trial number?
task_bt <- task_data %>% mutate(trigger_val = trigger_val_actual,
safe_val = safe_val_actual,
other_val = other_noise) %>%
group_by(s_num, trigger_val, safe_val, other_val, p_trigger) %>%
summarise(n_trials = n(), n_accept = sum(accept), prop_accept = sum(accept)/n())
# ... get a proportion of this...
# for each subject, what proportion of trials are not 1 or 0
task_prop_accept <- task_bt %>% group_by(s_num, prop_accept) %>% summarise(n_trials = n()) %>% filter(prop_accept %in% c(0, 0.5, 1))
# really need to look at the subject you scanned...
ggplot(task_prop_accept, aes(x = prop_accept, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_wrap(~s_num)
#s1_data <- task_data %>% filter(s_num == 1)
task_data$trigger_val_base %>% unique()
nt_data <- task_data %>% group_by(s_num,safe_val_base,trigger_val_base, p_trigger) %>% summarise(n_trials = n())
# could the problem be practice trials?
ggplot(nt_data %>% filter(trigger_val_base > 0, s_num == 8), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
ggplot(nt_data %>% filter(trigger_val_base < 0, s_num == 8), aes(x = p_trigger, y = n_trials)) +
geom_bar(stat = "identity", position = position_dodge()) +
facet_grid(safe_val_base ~ trigger_val_base)
View(data)
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
source("plot_funcs.R")
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
getwd()
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
setwd("C:/Users/erussek/forage_jsp/analysis")
rm(list = ls())
library(tidyr)
library(dplyr)
getwd()
library(zoo)
library(ggplot2)
library(ggpubr)
library(RcppRoll)
library(knitr)
getwd()
setwd("C:/Users/erussek/forage_jsp/analysis")
source("plot_funcs.R")
getwd()
source("plot_funcs.R")
source("plot_funcs.R")
source("plot_funcs.R")
data <- read.csv('data\\run3_data.csv')
#source("plot_funcs.R")
data <- read.csv('data\\run3_data.csv')
